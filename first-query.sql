/* Select all the table */
SELECT * FROM actor;

/* Select only two columns */
SELECT first_name, last_name from actor;

/* Assignament 1:
 	Select first, name, last name and email from customers
*/
SELECT first_name, last_name, email FROM customer;